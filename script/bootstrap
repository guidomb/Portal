#!/usr/bin/env ruby

require_relative 'script/lib/install_swiftlint'
require_relative 'script/lib/installers'

install_swiftlint

# Install CI environment dependencies
# proselint is a dependency needed by danger-prose plugin.
pip_install "proselint"

# Install Ruby gems
bundle_install
