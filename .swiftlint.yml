included: # paths to include during linting. `--path` is ignored if present.
  - Portal
excluded:
  - Portal/AutoGenerated

# Disabled Rules
disabled_rules:
  - trailing_whitespace
  - generic_type_name
  - todo

# Rules configuration
generic_type_name:
  - min_length: 1
  - max_length: 30
